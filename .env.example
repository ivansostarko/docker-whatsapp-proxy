# REQUIRED: your server's public IPv4 address
PUBLIC_IP=203.0.113.10

# Recommended: point a DNS name to your PUBLIC_IP
PROXY_HOSTNAME=proxy.example.com

# Image tag (pin if you want deterministic rollouts)
WHATSAPP_PROXY_IMAGE_TAG=latest

# Host bind ports
BIND_HTTP_PORT=80
BIND_HTTPS_PORT=443
BIND_XMPP_PORT=5222

# Optional
TZ=Europe/Zagreb
