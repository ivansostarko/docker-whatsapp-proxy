# Placeholder.
# Do NOT enable config override in docker-compose.yml until you have:
# 1) extracted the running container's HAProxy config, and
# 2) confirmed the correct config path for your image tag,
# 3) validated HAProxy starts cleanly with your changes.
